# MedEdge Backend — Python Dependencies

# ── Core Framework ────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9      # for file uploads

# ── Data / ORM ───────────────────────────────────────────────────────────────
sqlmodel==0.0.21
pydantic==2.8.2
pydantic-settings==2.4.0

# ── AI Providers ─────────────────────────────────────────────────────────────
openai>=1.40.0               # OpenAI + Ollama-compatible (OpenAI-compat API)
google-generativeai>=0.8.0   # Gemini + MedGemma via Google AI Studio
groq>=0.9.0                  # Groq ultra-fast inference (Llama, Mixtral)

# ── Speech-to-Text ───────────────────────────────────────────────────────────
faster-whisper==1.0.3        # Local offline STT (no internet needed)

# ── Environment & Config ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Logging & Observability ──────────────────────────────────────────────────
structlog==24.4.0

# ── Rate Limiting ────────────────────────────────────────────────────────────
slowapi==0.1.9
limits==3.13.0

# ── Security ─────────────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# ── HTTP ─────────────────────────────────────────────────────────────────────
httpx==0.27.0                # Async HTTP for health checks

# ── Optional: SMS Gateway ─────────────────────────────────────────────────────
# africastalking==1.3.2      # Uncomment when SMS is configured
# twilio==9.3.0              # Uncomment as alternative
